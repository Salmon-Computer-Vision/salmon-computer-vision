version: '3.8'

services:
  salmon_counter:
    image: ${IMAGE_REPO_HOST}/salmoncounter:${TAG}
    container_name: salmon_counter
    environment:
      - HOSTNAME
      - SMB_SHARE=${NETWORK_DRIVE_SHARE} # //server/share
    env_file:
      - ./.env
    volumes:
      - ${DRIVE}:/app/drive/hdd
      - /media/local_hdd:/app/drive/local_hdd # Fallback
      - /home/${USERNAME}/config:/app/config
    network_mode: host
    runtime: nvidia
    restart: always
    working_dir: /app
    command: ["/app/entrypoint.sh"]

